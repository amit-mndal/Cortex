# Local dev (default if this file is absent):
VITE_API_BASE=http://localhost:8000/api

# Production (set this in Vercel's project settings instead of committing
# a real value here) - point at your deployed Render backend:
# VITE_API_BASE=https://cortex-api.onrender.com/api
